-- Intentionally empty.
--
-- Wardrobe AI has no global product catalog, demo wardrobe, or shared user data.
-- Tests should create isolated auth users and fixtures, then remove them. Keeping
-- the default seed empty prevents a local reset from introducing misleading or
-- cross-user wardrobe records.

begin;
commit;
