# Observability

Propagate W3C trace context and `x-request-id` through Frontend, Backend, durable jobs, Worker, AI orchestration, providers, and database calls. Logs and metrics must never use raw prompts, signed URLs, user IDs, or object paths as attributes.

Initial service objectives and feature metrics are defined in `docs/architecture-and-performance-review-2026-08-12.md`.
