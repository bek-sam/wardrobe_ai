# ---------------------------------------------------------------------------
# Frontend (root src/) — browser-safe values only
# ---------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
BACKEND_URL=http://127.0.0.1:3001
# Exact origin used by signed private-upload URLs; included in the CSP. This is
# an origin, never a service-role key, provider token, or database credential.
NEXT_PUBLIC_STORAGE_ORIGIN=http://127.0.0.1:54321
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=false
NEXT_PUBLIC_EMAIL_MAGIC_LINK_ENABLED=false
NEXT_PUBLIC_CAPTCHA_ENABLED=false
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# ---------------------------------------------------------------------------
# Backend — sessions, public API, database and authorization
# ---------------------------------------------------------------------------
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PUBLIC_SIGNUP_ENABLED=false
AUTH_ACTION_SECRET=
AUTH_RATE_LIMIT_HMAC_SECRET=
TRUSTED_CLIENT_IP_HEADER=

# Backend and Worker call AI Orchestration through this private authenticated
# endpoint. Generate the shared workload token with: openssl rand -hex 32
AI_ORCHESTRATION_URL=http://127.0.0.1:3002
AI_SERVICE_TOKEN=

# Weather is deterministic application data work, not an AI-provider concern.
OPEN_METEO_FORECAST_URL=https://api.open-meteo.com/v1/forecast
OPEN_METEO_GEOCODING_URL=https://geocoding-api.open-meteo.com/v1/search
WEATHER_CACHE_TTL_SECONDS=900

# Private Storage ownership (Backend signs; Worker reads/writes).
WARDROBE_ORIGINALS_BUCKET=wardrobe-originals
WARDROBE_ITEMS_BUCKET=wardrobe-items
WARDROBE_LABELS_BUCKET=wardrobe-labels
WARDROBE_GENERATED_BUCKET=wardrobe-generated
PROFILE_REFERENCES_BUCKET=profile-references
SIGNED_URL_TTL_SECONDS=300

# Public request quotas enforced by Backend/database.
DAILY_IMAGE_LIMIT=20
DAILY_STYLIST_LIMIT=40
DAILY_PLANNER_LIMIT=10
STYLIST_RATE_LIMIT_PER_MINUTE=5
PLANNER_RATE_LIMIT_PER_MINUTE=2
IMAGE_RATE_LIMIT_PER_MINUTE=3
WARDROBE_QUERY_RATE_LIMIT_PER_MINUTE=20
INTENT_CLASSIFICATION_RATE_LIMIT_PER_MINUTE=10
IDENTITY_ASSESSMENT_DAILY_LIMIT=10
IDENTITY_ASSESSMENT_RATE_LIMIT_PER_MINUTE=3
VISUALIZATION_DOWNLOAD_RATE_LIMIT_PER_MINUTE=20

# ---------------------------------------------------------------------------
# Worker — durable claims, leases, retries and bounded vertical concurrency
# ---------------------------------------------------------------------------
WORKER_ENABLED_FAMILIES=storage-deletion,import,research,wardrobe-compilation,outfit-preview,outfit-visualization
WORKER_CONCURRENCY=2
WARDROBE_COMPILATION_MAX_CANDIDATES=
WARDROBE_COMPILATION_MAX_FOUNDATIONS_PER_BUCKET=
WARDROBE_CURATOR_MAX_CANDIDATES=40
WARDROBE_CURATOR_MAX_CALLS_PER_COMPILATION=2
WARDROBE_CURATOR_MAX_SELECTED_PER_NEW_ITEM=5
DAILY_CURATOR_CALL_LIMIT=20
OUTFIT_ANALYSIS_CACHE_TTL_DAYS=90
PREVIEW_MAX_AUTO_PER_UPLOAD=5
PREVIEW_DAILY_LIMIT=30
PREVIEW_MAX_QUEUED_PER_USER=5
PREVIEW_FREQUENTLY_SUGGESTED_THRESHOLD=3

# Provider-neutral cache/audit versions. Bump the relevant value whenever AI
# policy changes in a way that must invalidate cached output.
AI_CATALOG_POLICY_VERSION=catalog@v1
AI_RESEARCH_POLICY_VERSION=research@v1
AI_CURATOR_POLICY_VERSION=curator@v1
AI_IMAGE_POLICY_VERSION=image@v1

# ---------------------------------------------------------------------------
# AI Orchestration only — never inject these into Frontend, Backend, or Worker
# ---------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_API_BASE_URL=https://api.openai.com/v1
OPENAI_VISION_MODEL=gpt-5.5
OPENAI_STYLIST_MODEL=gpt-5.5
OPENAI_PLANNER_MODEL=gpt-5.5
OPENAI_RESEARCH_MODEL=gpt-5.5
OPENAI_CURATOR_MODEL=gpt-5.5
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_QUALITY=high
OPENAI_VISUALIZATION_QA_MODEL=
OPENAI_IMAGE_CAPABILITY_PROFILE=auto_fidelity
# Development/test only; production must use openai.
OUTFIT_VISUALIZATION_PROVIDER=openai
OUTFIT_VISUALIZATION_FAKE_OUTCOME=
